{
  "name": "reactdb-root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd libraryapp && npm install && npm run build && cp -r dist ../public"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.4"
  }
}
